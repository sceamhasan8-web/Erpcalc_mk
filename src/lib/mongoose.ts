/**
 * MongoDB / Mongoose connection handler
 * Disabled: Application exclusively uses Firebase Firestore cloud database.
 */
export async function connectToDatabase() {
  return null;
}

export default connectToDatabase;
